{
	"name": "obsidian-entry-timestamp",
	"version": "0.1.1",
	"description": "Auto-timestamps task entries and provides URI handler for external entry creation",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "node esbuild.config.mjs production"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"esbuild": "^0.24.0",
		"obsidian": "latest",
		"tslib": "^2.8.0",
		"typescript": "^5.7.0"
	}
}
